name: Deploy to Github Pages

on:
  push:
    branches: [ develop ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - run: yarn global add @quasar/cli
    - run: yarn

  build:
    runs-on: ubuntu-latest
    steps:
    - run: quasar build
    - run: yarn dev
