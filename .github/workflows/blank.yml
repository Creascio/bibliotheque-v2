name: Deploy to Github Pages

on:
  push:
    branches: [ develop ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - run: yarn install
    - run: cd ..
    - run: yarn build:spa
    - run: yarn dev

   
