name: Deploy to Github Pages

on:
  push:
    branches: [ develop ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - run: yarn install

  build:
    runs-on: ubuntu-latest
    steps:
    - run: cd ..
    - run: yarn build:spa
    - run: yarn dev
