name: Deploy to Github Pages

on:
  push:
    branches: [ develop ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - run: yarn add @quasar/cli

  build:
    runs-on: ubuntu-latest
    steps:
    - run: quasar build
    - run: yarn dev
